INCLUDE_DIRECTORIES( . )

FILE(GLOB_RECURSE SRC_LIST *.cpp)

AUX_SOURCE_DIRECTORY(${SERVER_SOURCE_DIR}/common SRC_LIST)
AUX_SOURCE_DIRECTORY(${SERVER_SOURCE_DIR}/common/log SRC_LIST)

ADD_EXECUTABLE(log_test ${SRC_LIST})

TARGET_LINK_LIBRARIES(log_test pthread) 
