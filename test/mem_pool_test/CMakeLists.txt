INCLUDE_DIRECTORIES( . )

FILE(GLOB_RECURSE SRC_LIST *.cpp)

AUX_SOURCE_DIRECTORY(${SERVER_SOURCE_DIR}/common SRC_LIST)

ADD_EXECUTABLE(mem_pool_test ${SRC_LIST})

TARGET_LINK_LIBRARIES(mem_pool_test pthread) 
